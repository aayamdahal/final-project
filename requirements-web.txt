# Flask 1.1.2 predates the 2.0 releases of its own dependencies, which dropped
# APIs it relies on (e.g. itsdangerous.json). Pin the compatible versions.
Flask==1.1.2
Werkzeug==1.0.1
Jinja2==2.11.3
itsdangerous==1.1.0
click==7.1.2
MarkupSafe==1.1.1
gunicorn==20.0.4
